spring:
  application:
    name: notificacao-grupos
#  web:
#    cors:
#      allowed-origins: http://localhost:1664,http://localhost:4200
#      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
#      allowed-headers: "*"
#      allow-credentials: true

server:
  port: ${PORT:3333}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.aerolinea.transporte.notificacao_grupos: DEBUG
